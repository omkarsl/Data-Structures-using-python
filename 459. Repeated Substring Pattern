class Solution(object):
    def repeatedSubstringPattern(self, s):
        n=len(s)
        for i in range(1,n):
            sub=s[:i]
            times=n//len(sub)
            if sub*times==s:
                return True
        return False
